<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--suppress SqlNoDataSourceInspection, SqlDialectInspection -->
<mapper namespace="org.clever.security.mapper.RoleMapper">
    <select id="findRolesByUid" resultType="java.lang.String">
        select
            b.name
        from user_role a left join role b on (a.role_id=b.id and a.domain_id=b.domain_id)
        where a.domain_id=#{domainId} and a.uid=#{uid}
    </select>
</mapper>