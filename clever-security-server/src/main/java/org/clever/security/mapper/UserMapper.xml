<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--suppress SqlNoDataSourceInspection, SqlDialectInspection -->
<mapper namespace="org.clever.security.mapper.UserMapper">
    <select id="pageQuery" resultType="org.clever.security.entity.User">
        select
        a.uid,
        a.login_name,
        a.telephone,
        a.email,
        a.expired_time,
        a.enabled,
        a.nickname,
        a.avatar,
        a.register_channel,
        a.from_source,
        a.description,
        a.create_at,
        a.update_at
        from user a
        left join user_domain b on (a.uid = b.uid)
        <where>
            <if test="query.domainId!=null">
                and b.domain_id=#{query.domainId}
            </if>
            <if test="query.keyword!=null and query.keyword!=''">
                and (a.login_name like concat('%', #{query.keyword}, '%')
                or a.telephone like concat('%', #{query.keyword}, '%')
                or a.email like concat('%', #{query.keyword}, '%')
                or a.nickname like concat('%', #{query.keyword}, '%'))
            </if>
            <if test="query.createAtStart!=null">
                and a.create_at>=#{query.createAtStart}
            </if>
            <if test="query.createAtEnd!=null">
                <![CDATA[ and a.create_at<=#{query.createAtEnd} ]]>
            </if>
        </where>
    </select>
</mapper>