<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--suppress SqlNoDataSourceInspection, SqlDialectInspection -->
<mapper namespace="org.clever.security.mapper.UserLoginLogMapper">
    <select id="pageQuery" resultType="org.clever.security.dto.response.admin.UserLoginLogQueryRes">
        select
        a.*,
        b.name as domainName,
        c.nickname,
        c.login_name as loginName,
        c.telephone,
        c.email
        from user_login_log a
        left join domain b on (a.domain_id = b.id)
        left join user c on (a.uid = c.uid)
        <where>
            <if test="query.id!=null">
                and a.id=#{query.id}
            </if>
            <if test="query.domainId!=null">
                and a.domain_id=#{query.domainId}
            </if>
            <if test="query.loginIp!=null and query.loginIp!=''">
                and a.login_ip like concat('%', #{query.loginIp}, '%')
            </if>
            <if test="query.loginChannel!=null">
                and a.login_channel = #{query.loginChannel}
            </if>
            <if test="query.loginType!=null">
                or a.login_type = #{query.loginType}
            </if>
            <if test="query.loginState!=null">
                or a.login_state = #{query.loginState}
            </if>
            <if test="query.loginTimeStart!=null">
                and a.login_time>=#{query.loginTimeStart}
            </if>
            <if test="query.loginTimeEnd!=null">
                <![CDATA[ and a.login_time<=#{query.loginTimeEnd} ]]>
            </if>
        </where>
    </select>
</mapper>