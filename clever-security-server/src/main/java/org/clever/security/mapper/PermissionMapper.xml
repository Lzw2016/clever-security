<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--suppress SqlNoDataSourceInspection, SqlDialectInspection -->
<mapper namespace="org.clever.security.mapper.PermissionMapper">
    <select id="findPermissionByUid" resultType="java.lang.String">
        select
            distinct c.str_flag
        from user_role a
            left join role_permission b on (b.role_id=a.role_id and b.domain_id=a.domain_id)
            left join permission c on (c.id=b.permission_id and c.domain_id=b.domain_id)
        where c.str_flag is not null and a.domain_id=#{domainId} and a.uid=#{uid}
    </select>
</mapper>