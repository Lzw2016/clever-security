<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--suppress SqlNoDataSourceInspection, SqlDialectInspection -->
<mapper namespace="org.clever.security.mapper.ServerAccessTokenMapper">
    <select id="pageQuery" resultType="org.clever.security.dto.response.admin.ServerAccessTokenQueryRes">
        select a.*, b.name as domain_name from server_access_token a left join domain b on (a.domain_id = b.id)
        <where>
            <if test="query.domainId!=null">
                and a.domain_id=#{query.domainId}
            </if>
            <if test="query.tag!=null and query.tag!=''">
                and a.tag like concat('%', #{query.tag}, '%')
            </if>
            <if test="query.tokenName!=null and query.tokenName!=''">
                and a.token_name like concat('%', #{query.tokenName}, '%')
            </if>
            <if test="query.tokenValue!=null and query.tokenValue!=''">
                and a.token_value=#{query.tokenValue}
            </if>
            <if test="query.expiredTimeStart!=null">
                and a.expired_time>=#{query.expiredTimeStart}
            </if>
            <if test="query.expiredTimeEnd!=null">
                <![CDATA[ and a.expired_time<=#{query.expiredTimeEnd} ]]>
            </if>
            <if test="query.disable!=null">
                and a.disable=#{query.disable}
            </if>
            <if test="query.createAtStart!=null">
                and a.create_at>=#{query.createAtStart}
            </if>
            <if test="query.createAtEnd!=null">
                <![CDATA[ and a.create_at<=#{query.createAtEnd} ]]>
            </if>
        </where>
    </select>
</mapper>